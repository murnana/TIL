# 自分がGitをインストールした後に設定しているもの一覧

## global設定

```bat
git config --global core.precomposeunicode true
git config --global core.editor vim
git config --global core.autocrlf false
git config --global core.ignorecase true
```
